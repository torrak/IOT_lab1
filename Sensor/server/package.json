{
	"name": "server",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start:dev": "concurrently \"node index.js\" \"cd ../web && npm run dev\"",
		"dev": "node index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^17.2.1",
		"express": "^4.18.2",
		"mqtt": "^5.14.0"
	},
	"devDependencies": {
		"concurrently": "^8.2.2",
		"nodemon": "^3.0.3"
	}
}
